<h1><%= @product.name %></h1>

<p>₹<%= @product.price %></p>

<% if current_user.admin? %>
  <p><strong>Qty: </strong><%= @product.quantity %></p>

  <ul>
    <li><%= link_to "Edit", edit_product_path(@product) %></li>
    <li><%= link_to "Destroy", product_path(@product), data: {
                      turbo_method: :delete,
                      turbo_confirm: "Are you sure?"
                    } %></li>

  </ul>
<% else %>
  <%= form_with model: @order_product do |f| %>
    <div>
      <%= f.label :quantity, "Quantity: " %>
      <%= f.number_field :quantity, min: 1, max: @product.quantity %>
    </div><br>

    <div>

      <%= button_to "Add to Cart", order_products_path(product_id: @product.id, customer_id: current_user.id), :method => :post %>
      <%= f.submit "submit", class:"btn btn-primary" %>
    </div>
  <% end %>
<% end %>

<!-- Check if product_id, cart_id, customer_id ja rhi hai ki nhi. -->